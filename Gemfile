source "https://rubygems.org"

# GitHub Pages에서 제공하는 Jekyll, 플러그인, 테마 환경을 그대로 씁니다
gem "github-pages", group: :jekyll_plugins

# include_cached 태그를 처리해 주는 플러그인
gem "jekyll-include-cache", group: :jekyll_plugins
